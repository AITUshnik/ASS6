package factory;
//First step for implementing/creating factory method is create interface which will be follow by all products
//So, its a first step:
public interface Button {
    //Common interface for all buttons.
        void render();
        void onClick();
    }

